version: '3'

services:
  commerce_offer_dev_postgres:
    image: "postgres:10-alpine"
    ports:
      - "5432:5432"
    volumes:
      - ./postgresql:/docker-entrypoint-initdb.d/
    environment:
      SHOP_POSTGRES_USER: shop
      SHOP_POSTGRES_PASSWORD: shop
      SHOP_POSTGRES_DB: shop
      SHOP_POSTGRES_DB_SCHEMA: shop
    container_name: shop_postgres_5432
#  app:
#    image: 7648f7fffa68
#    ports:
#      - "8082:8080"
#    environment:
#      JDBC_URL: jdbc:postgresql://localhost:5432/shop